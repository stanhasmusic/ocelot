# Godot 4+ specific ignores
.godot/
/android/

# Export output (large binaries — don't commit)
/build/

# Windows metadata
desktop.ini

# Claude Code local settings (machine-specific)
.claude/settings.local.json
